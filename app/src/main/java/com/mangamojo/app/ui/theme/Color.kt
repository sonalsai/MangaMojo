package com.mangamojo.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette — a calm violet accent that reads well over both light and the
// dark, near-black surfaces a manga reader spends most of its time on.
val Violet = Color(0xFF7C5CFF)
val VioletLight = Color(0xFFB7A4FF)
val VioletDark = Color(0xFF4B33C2)

// Dark theme surfaces (default for a reader).
val DarkBackground = Color(0xFF0E0E14)
val DarkSurface = Color(0xFF16161F)
val DarkSurfaceVariant = Color(0xFF24242F)
val DarkOnSurface = Color(0xFFE7E6EF)
val DarkOnSurfaceVariant = Color(0xFFB6B5C4)

// Light theme surfaces.
val LightBackground = Color(0xFFF7F6FB)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFECEAF4)
val LightOnSurface = Color(0xFF1B1B22)
val LightOnSurfaceVariant = Color(0xFF55545F)
